export interface User {
    id: string;
    email: string;
    firstName: string;
    lastName: string;
    createdAt: string;
    // user_metadata: string;
    // aud: string;
    // created_at: string;
}